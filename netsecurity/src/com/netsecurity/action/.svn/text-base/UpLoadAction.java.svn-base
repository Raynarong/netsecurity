package com.netsecurity.action;

import com.opensymphony.xwork2.ActionSupport;

public class UpLoadAction extends ActionSupport
{
	public String enterUploadPage() throws Exception {
		//获得省对象 目前是1
	//	ProvinceInfo province = new ProvinceDao().getProvinceById(1);
		
		//保存数据
	//	ServletActionContext.getRequest().setAttribute("province", province);
		
		return SUCCESS;
	}
}
